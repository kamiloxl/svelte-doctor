name: "svelte-doctor-cli"
description: "Diagnose your Svelte 5 codebase. 0–100 health score."
branding:
  icon: "activity"
  color: "orange"

inputs:
  directory:
    description: "Project root to scan."
    required: false
    default: "."
  diff:
    description: "Base branch to diff against (e.g. main). When set, only changed files are scanned."
    required: false
  fail-on:
    description: "Fail the action when diagnostics of this severity or worse are found: error | warning | none."
    required: false
    default: "none"
  github-token:
    description: "GitHub token. When provided on pull_request events, diagnostics are posted (and updated) as a single PR comment."
    required: false
  project:
    description: "Workspace project(s) to scan (comma-separated)."
    required: false
  node-version:
    description: "Node.js version to use."
    required: false
    default: "22"
  audit:
    description: "Run npm/pnpm audit and report vulnerable dependencies as security diagnostics."
    required: false
    default: "false"
  scope:
    description: "Rule scope: recommended | all | security."
    required: false
    default: "all"
  sarif:
    description: "Emit a SARIF 2.1.0 report and (when 'true') upload it to GitHub Code Scanning."
    required: false
    default: "false"
  upload-sarif:
    description: "When sarif=true, also upload the report via github/codeql-action/upload-sarif. Requires `security-events: write`."
    required: false
    default: "true"

outputs:
  score:
    description: "Health score 0–100 (or empty when score could not be computed)."
    value: ${{ steps.score.outputs.score }}
  sarif-path:
    description: "Path to the generated SARIF file (only set when sarif=true)."
    value: ${{ steps.run.outputs.sarif-path }}

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}

    - name: Run svelte-doctor-cli
      id: run
      shell: bash
      env:
        DIRECTORY: ${{ inputs.directory }}
        DIFF_BASE: ${{ inputs.diff }}
        FAIL_ON: ${{ inputs.fail-on }}
        PROJECT: ${{ inputs.project }}
        AUDIT: ${{ inputs.audit }}
        SCOPE: ${{ inputs.scope }}
        SARIF: ${{ inputs.sarif }}
      run: |
        set -e
        REPORT_PATH="${RUNNER_TEMP:-/tmp}/svelte-doctor-cli-report.json"
        SARIF_PATH="${RUNNER_TEMP:-/tmp}/svelte-doctor-cli.sarif"
        ARGS=("$DIRECTORY" "--annotations" "--fail-on" "$FAIL_ON" "--yes")
        if [ -n "$DIFF_BASE" ]; then
          ARGS+=("--diff" "$DIFF_BASE")
        fi
        if [ -n "$PROJECT" ]; then
          ARGS+=("--project" "$PROJECT")
        fi
        if [ -n "$SCOPE" ]; then
          ARGS+=("--scope" "$SCOPE")
        fi
        if [ "$AUDIT" = "true" ]; then
          ARGS+=("--audit")
        fi
        npx -y svelte-doctor-cli@latest "${ARGS[@]}" || true
        JSON_ARGS=("$DIRECTORY" "--json" "--yes")
        if [ -n "$DIFF_BASE" ]; then
          JSON_ARGS+=("--diff" "$DIFF_BASE")
        fi
        if [ -n "$PROJECT" ]; then
          JSON_ARGS+=("--project" "$PROJECT")
        fi
        if [ -n "$SCOPE" ]; then
          JSON_ARGS+=("--scope" "$SCOPE")
        fi
        if [ "$AUDIT" = "true" ]; then
          JSON_ARGS+=("--audit")
        fi
        npx -y svelte-doctor-cli@latest "${JSON_ARGS[@]}" > "$REPORT_PATH"
        echo "report-path=$REPORT_PATH" >> "$GITHUB_OUTPUT"
        if [ "$SARIF" = "true" ]; then
          SARIF_ARGS=("$DIRECTORY" "--sarif" "--sarif-file" "$SARIF_PATH" "--yes")
          if [ -n "$DIFF_BASE" ]; then
            SARIF_ARGS+=("--diff" "$DIFF_BASE")
          fi
          if [ -n "$PROJECT" ]; then
            SARIF_ARGS+=("--project" "$PROJECT")
          fi
          if [ -n "$SCOPE" ]; then
            SARIF_ARGS+=("--scope" "$SCOPE")
          fi
          if [ "$AUDIT" = "true" ]; then
            SARIF_ARGS+=("--audit")
          fi
          npx -y svelte-doctor-cli@latest "${SARIF_ARGS[@]}" || true
          echo "sarif-path=$SARIF_PATH" >> "$GITHUB_OUTPUT"
        fi

    - name: Upload SARIF to GitHub Code Scanning
      if: ${{ inputs.sarif == 'true' && inputs.upload-sarif == 'true' }}
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: ${{ steps.run.outputs.sarif-path }}
        category: svelte-doctor

    - name: Compute score
      id: score
      if: always()
      shell: bash
      env:
        DIRECTORY: ${{ inputs.directory }}
        PROJECT: ${{ inputs.project }}
      run: |
        # --score is an output-collection step, not a gate. Force --fail-on none
        # so the composite action doesn't fail under `set -e -o pipefail`.
        SCORE_ARGS=("$DIRECTORY" "--score" "--fail-on" "none" "--yes")
        if [ -n "$PROJECT" ]; then
          SCORE_ARGS+=("--project" "$PROJECT")
        fi
        SCORE=$(npx -y svelte-doctor-cli@latest "${SCORE_ARGS[@]}" 2>/dev/null | tail -1 | tr -d '[:space:]') || true
        if [[ -n "$SCORE" && "$SCORE" =~ ^[0-9]+$ ]]; then
          echo "score=$SCORE" >> "$GITHUB_OUTPUT"
        fi

    - name: Comment on PR
      if: ${{ inputs.github-token != '' && github.event_name == 'pull_request' }}
      uses: actions/github-script@v7
      env:
        REPORT_PATH: ${{ steps.run.outputs.report-path }}
        SVELTE_DOCTOR_SCORE: ${{ steps.score.outputs.score }}
      with:
        github-token: ${{ inputs.github-token }}
        script: |
          const fs = require('fs');
          const report = JSON.parse(fs.readFileSync(process.env.REPORT_PATH, 'utf8'));
          const score = process.env.SVELTE_DOCTOR_SCORE || report?.score?.score;
          const label = report?.score?.label;
          const total = report?.summary?.total ?? 0;
          const marker = '<!-- svelte-doctor-cli -->';
          const lines = [];
          lines.push(marker);
          lines.push('### svelte-doctor-cli');
          lines.push('');
          lines.push(`**Score:** ${score ?? 'n/a'} / 100${label ? ` — ${label}` : ''}`);
          lines.push(`**Issues:** ${total}`);
          if (total > 0) {
            lines.push('');
            lines.push('| File | Line | Severity | Rule | Message |');
            lines.push('| --- | ---: | --- | --- | --- |');
            for (const d of report.diagnostics.slice(0, 30)) {
              lines.push(`| \`${d.file}\` | ${d.line} | ${d.severity} | \`${d.ruleId}\` | ${d.message.replace(/\|/g, '\\|')} |`);
            }
            if (report.diagnostics.length > 30) {
              lines.push('');
              lines.push(`…and ${report.diagnostics.length - 30} more.`);
            }
          }
          const body = lines.join('\n');

          const { data: comments } = await github.rest.issues.listComments({
            ...context.repo,
            issue_number: context.issue.number,
          });
          const previous = comments.find((c) => c.body?.startsWith(marker));
          if (previous) {
            await github.rest.issues.updateComment({
              ...context.repo,
              comment_id: previous.id,
              body,
            });
          } else {
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body,
            });
          }
